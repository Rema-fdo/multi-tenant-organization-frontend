// Deprecated. Login/sign-up is now handled inline within each role page
// (app/admin/page.tsx and app/user/page.tsx) against the new auth contract
// ({ accessToken, refreshToken, user }) and domain-based sign-up. This file is
// intentionally left as an empty module to avoid a stale, non-compiling import.
export {};
